{% block title %}
    Admin évenements
{% endblock %}

{% block body %}
    <a href="{{ path('admin_article_new') }}" class="btn btn-outline-secondary card-link font-weight-bold text-dark">Ajouter</a>
        <table>
            {% for article in articles %}

             <tr>
                 <td>{{ article.title }}</td>
                <td>{{ include('admin/article/_delete_form.html.twig') }}</td>
                <td><a href="{{ path('admin_article_edit',{id:article.id}) }}" class="btn btn-outline-secondary card-link font-weight-bold text-dark">
                    <i class="far fa-edit"></i>
                    Modifier</a>
                </td>
              </tr>

            {% endfor %}
    </table>
{% endblock %}
