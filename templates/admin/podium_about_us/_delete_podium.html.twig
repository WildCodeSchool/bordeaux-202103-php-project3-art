<form method="post" action="{{ path('admin_delete_external_podium', {position: loop.index}) }}"
{#      onsubmit="return confirm('Retirer cet article externe du podium ?');"#}
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete_external_podium' ~ loop.index) }}">
    <button class="btn-admin card-link text-dark"><i class="fas fa-user-minus"></i> Retirer</button>
</form>