{% block title %}
    Admin évenements
{% endblock %}

{% block body %}
    <a href="{{ path('admin_happening_new') }}" class="btn btn-outline-secondary card-link font-weight-bold text-dark">Ajouter</a>
        <table>
            {% for happening in happenings %}

             <tr>
                 <td>{{ happening.title }}</td>
                <td>{{ include('admin/happening/_delete_form.html.twig') }}</td>
                <td><a href="{{ path('admin_happening_edit',{id:happening.id}) }}" class="btn btn-outline-secondary card-link font-weight-bold text-dark">
                    <i class="far fa-edit"></i>
                    Modifier</a>
                </td>
              </tr>

            {% endfor %}
    </table>
{% endblock %}
