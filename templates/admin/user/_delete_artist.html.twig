<form method="post" action="{{ path('admin_delete_user', {'id': user.id}) }}"
      onsubmit="return confirm('Voulez-vous changer le statut du compte de cet.te utilisat.eur.rice ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn"><i class="fas fa-gavel"></i> {{ (user.isActive) ? 'Désactiver' : 'Activer' }}</button>
</form>