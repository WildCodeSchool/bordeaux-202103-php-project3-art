<div class="container-fluid-nav">
<nav class="navbar navbar-expand-lg navbar-dark bg-deko">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-link font-weight-bold" href="#">LES ARTISTES</a>
            <a class="nav-link font-weight-bold" href="#">L'ASSO</a>
            <a class="nav-link font-weight-bold" href="#">L'ACTUALITE</a>
            <a class="nav-link font-weight-bold" href="#">BLOG</a>
            <a href="/"><img src="{{ asset('build/logo.png') }}" alt="logo arts d'eko" width="100px"></a>
            <a class="nav-link font-weight-bold" href="#">CONTACT</a>
            <a class="nav-link font-weight-bold" href="{{ path('app_login') }}">{{ (app.user) ? "MON PROFIL" : "CONNEXION" }}</a>
            <a class="nav-link font-weight-bold" href="#">RECHERCHE</a>
        </div>
    </div>
</nav>
</div>
